{
  "schema_version": "public-verification/v1",
  "timestamp": "2026-08-12T00:00:00Z",
  "scope": "safe offline source, documentation, notebook, and report checks; no Qiskit, Aer, worker, EICrecon, ROOT, or scientific workload execution",
  "historical_record": {
    "command": "PYTHONPATH=src python3 -m pytest tests/python/test_wire_request_digest.py",
    "result": "7 pytest tests passed locally",
    "classification": "local unit verification, not campaign measurement"
  },
  "current_checks": {
    "pytest": {
      "command": "PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src python3 -m pytest -p no:cacheprovider tests/python/test_wire_request_digest.py",
      "result": "10 passed in 0.13s",
      "safety_basis": "Qiskit and Aer imports are deferred to sampling functions not called by this suite."
    },
    "bash_syntax": {
      "command": "bash -n scripts/run-worker scripts/run-shadow scripts/run-active",
      "result": "passed"
    },
    "notebooks": {
      "command": "jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=120 notebooks/<name>.ipynb",
      "result": "both bundled-evidence notebooks executed successfully; each has 2 code-cell outputs and no errors"
    },
    "latex": {
      "command": "make -C report clean && make -C report",
      "result": "passed; report PDF rebuilt with 10 pages"
    },
    "shellcheck": "unavailable locally; CI will run it"
  }
}
